package main

/*
// Инциализация и подгрузка переменных окружения
func init() {
	err := godotenv.Load(".." + string(os.PathSeparator) + "config" + string(os.PathSeparator) + "config.env")
	if err != nil {
		log.Fatal("No conf file provided. Check link")
	}

}
*/
